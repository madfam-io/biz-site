{
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "buildCommand": "cd ../.. && pnpm build --filter=@madfam/web",
  "installCommand": "pnpm install --frozen-lockfile",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}